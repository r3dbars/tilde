# Compatibility Guide

App compatibility logic lives here.

- Keep this layer pure Swift.
- Do not store or log raw user text.
- Model weird app behavior as explicit profiles or validation policies.
- Add tests for every new fallback or override.
