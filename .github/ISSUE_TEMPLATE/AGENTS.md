# Issue Template Guide

Use issue forms for structured beta feedback.

- Require build, app, permission state, expected behavior, actual behavior,
  reproduction steps, and severity.
- Diagnostics must be explicit opt-in.
- Never make raw typed text a required field.
