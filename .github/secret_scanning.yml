# The redaction evaluator's corpus deliberately contains secret-shaped
# strings (fake AWS keys, GitHub tokens, private-key blocks) so the
# redaction rules can be scored. They are generated by
# script/generate_redaction_corpus.py and are not credentials.
paths-ignore:
  - "script/testdata/**"
  - "Tests/**"
