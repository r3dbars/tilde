# Assets Folder Guide

Use this folder for project-owned visual assets.

- Keep source images checked in when scripts or docs reference them.
- Do not replace generated assets silently; add a new version when testing a new direction.
- Keep GitHub-facing assets in `GitHub/` and app icon sources in `AppIcon/`.
