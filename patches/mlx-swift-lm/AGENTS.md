# MLX Swift LM Patch Guide

Patches for `mlx-swift-lm` live here.

- Keep patch names specific.
- Include only dependency changes needed by this app.
- Re-check against the pinned package version in `Package.resolved`.
- Do not mix unrelated dependency experiments in one patch.
